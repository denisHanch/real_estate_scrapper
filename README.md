# Scrap real estate web pages
## How to run

1) Clone repo to local. 
2) In the root create `.env` file. Fill in the credentials in accordance to `.env_template` file
3) Run docker in the root folder:  
```
docker-compose up
```